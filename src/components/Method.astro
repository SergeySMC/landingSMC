---
const facts = [
  { title: 'Обход защиты', text: 'Вы видите суть сразу, без долгих часовых объяснений своей предыстории.' },
  { title: 'Только наглядные факты', text: 'Никаких гаданий. Вы сами называете то, что проявила карта.' },
  { title: 'Переводчик для тела', text: 'МАК помогают быстро перевести физический симптом или зажим на понятный язык слов.' },
  { title: 'Безопасность', text: 'Бережная атмосфера и комфортный для вас темп — с полным уважением к личным границам.' },
];
---

<section id="mak" class="method">
  <div class="grid">
    <div class="text-col">
      <p class="kicker">Метод</p>
      <h2>Почему МАК помогают увидеть то, что сложно сформулировать</h2>
      <p>Метафорические ассоциативные карты — проективный инструмент, который используется в психологии более 100 лет. Образ помогает взглянуть на привычную ситуацию с другой стороны и заметить то, что не всегда удаётся сразу выразить словами: чувства, мысли, внутренние противоречия и ассоциации.</p>
      <p>У карт нет правильных трактовок и фиксированных значений. Каждый человек воспринимает изображение через собственный опыт и находит в нём то, что откликается именно ему.</p>
      <p>МАК не предсказывают будущее и не предлагают готовых ответов. Они помогают исследовать свою ситуацию, увидеть новые связи и лучше понять, что с вами происходит.</p>
    </div>
    <div class="facts-col">
      <p class="facts-heading">Ключевые факты:</p>
      {facts.map((f) => (
        <div class="fact">
          <h3>{f.title}</h3>
          <p>{f.text}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .method {
    padding: clamp(30px, 4.1vw, 50px) clamp(20px, 5vw, 72px);
    max-width: 1120px;
    margin: 0 auto;
    border-top: 1px solid var(--line);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 310px), 1fr));
    gap: clamp(32px, 6vw, 84px);
    align-items: start;
  }

  .kicker {
    margin: 0 0 18px;
    font-size: 12px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent-soft);
  }

  h2 {
    margin: 0 0 24px;
    font-family: Lora, Georgia, serif;
    font-weight: 400;
    font-size: 28px;
    line-height: 1.22;
  }

  .text-col p:not(.kicker) {
    font-size: 15.5px;
    line-height: 1.7;
    color: var(--ink2);
  }

  .text-col p:not(.kicker) + p:not(.kicker) {
    margin-top: 18px;
  }

  .facts-col {
    display: flex;
    flex-direction: column;
    gap: 26px;
  }

  .facts-heading {
    margin: 0;
    font-family: Lora, Georgia, serif;
    font-size: 18px;
    font-weight: 500;
  }

  .fact {
    border-left: 1px solid var(--line2);
    padding-left: 22px;
  }

  .fact h3 {
    margin: 0 0 7px;
    font-size: 16px;
    font-weight: 500;
  }

  .fact p {
    margin: 0;
    font-size: 15.5px;
    line-height: 1.6;
    color: var(--muted);
  }
</style>
