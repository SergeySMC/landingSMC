---
const featuresTitle = 'Преимущества:';
const features = [
  'Работает прямо в браузере, с компьютера и смартфона — никаких сторонних подключений и программ',
  'Общая раскладка: вы сами кликаете и открываете карты в реальном времени.',
  'Полная конфиденциальность сессий.',
];
---

<section class="platform">
  <div class="inner">
    <div class="grid">
      <div class="text-col">
        <p class="kicker">«Инструмент» (Авторская платформа)</p>
        <h2>Интерактивная платформа для онлайн-сессий</h2>
        <p class="desc">Сессии проходят на авторской онлайн-платформе. В едином пространстве совмещены видеосвязь и интерактивный стол с МАК-картами. Здесь же вы можете отмечать свои эмоции, работать с Колесом баланса и наглядно отслеживать свой прогресс от встречи к встрече.</p>
        <div class="features">
          <p class="features-title">{featuresTitle}</p>
          {features.map((f) => (
            <div class="feature">
              <span class="dot"></span>
              <span>{f}</span>
            </div>
          ))}
        </div>
      </div>
      <div class="visual-col">
        <div class="placeholder">
          <span>скриншот платформы: видеозвонок + раскладка карт</span>
        </div>
        <a href="https://soulmindcards.com/" target="_blank" rel="noopener" class="btn-outline">Перейти на платформу</a>
      </div>
    </div>
  </div>
</section>

<style>
  .platform {
    background: var(--surface);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: clamp(30px, 4.1vw, 50px) clamp(20px, 5vw, 72px);
  }

  .inner {
    max-width: 1120px;
    margin: 0 auto;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 310px), 1fr));
    gap: clamp(32px, 6vw, 84px);
    align-items: center;
  }

  .kicker {
    margin: 0 0 18px;
    font-size: 12px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent-soft);
  }

  h2 {
    margin: 0 0 22px;
    font-family: Lora, Georgia, serif;
    font-weight: 400;
    font-size: clamp(25px, 3.4vw, 36px);
    line-height: 1.24;
  }

  .desc {
    margin: 0 0 28px;
    font-size: 17px;
    line-height: 1.7;
    color: var(--ink2);
  }

  .features-title {
    margin: 0 0 8px;
    font-weight: 500;
    font-size: 15.5px;
    color: var(--ink);
  }

  .features {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .feature {
    display: flex;
    gap: 14px;
    align-items: baseline;
  }

  .dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--accent);
    flex: 0 0 auto;
  }

  .feature span:last-child {
    font-size: 16px;
    line-height: 1.55;
    color: var(--ink2);
  }

  .btn-outline {
    margin-top: 28px;
    padding: 14px 28px;
    border-radius: 999px;
    border: 1px solid var(--accent);
    color: var(--accent);
    font-size: 15.5px;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    transition: background 0.15s, color 0.15s;
  }

  .btn-outline:hover {
    background: var(--accent);
    color: var(--on-accent);
  }

  .placeholder {
    height: clamp(220px, 29vw, 330px);
    background-color: var(--ph);
    background-image: repeating-linear-gradient(135deg, var(--ph-line) 0 1px, transparent 1px 11px);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 20px;
  }

  .placeholder span {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    color: var(--muted);
    background: var(--surface);
    padding: 8px 14px;
    text-align: center;
  }

  .visual-col {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
</style>
